package cryodunere.generated

import spice86.emulator.machine.Machine
import spice86.emulator.reverseengineer.MemoryBasedDataStructureWithBaseAddress
import spice86.emulator.memory.SegmentedAddress

open class GlobalsOnCsSegment0x2538(machine: Machine) :
  MemoryBasedDataStructureWithBaseAddress(machine.getMemory(), 0x2538 * 0x10) {
  // Getters and Setters for address 0x2538:0x18A/0x2550A.
  var word16_2538_018A_MouseCursorAddressInVram: Int
    // Operation not registered by running code
    get() = getUint16(0x18A)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x18A, value)

  // Getters and Setters for address 0x2538:0x18C/0x2550C.
  var byte8_2538_018C_ColumnsOfMouseCursorCount: Int
    // Was accessed via the following registers: CS
    get() = getUint8(0x18C)
    // Operation not registered by running code
    set(value) = setUint8(0x18C, value)

  var word16_2538_018C_ColumnsOfMouseCursorCount: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x18C)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x18C, value)

  // Getters and Setters for address 0x2538:0x18E/0x2550E.
  var word16_2538_018E_LinesOfMouseCursorCount: Int
    // Operation not registered by running code
    get() = getUint16(0x18E)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x18E, value)

  // Getters and Setters for address 0x2538:0x190/0x25510.
  var word16_2538_0190: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x190)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x190, value)

  // Getters and Setters for address 0x2538:0x192/0x25512.
  var word16_2538_0192: Int
    // Operation not registered by running code
    get() = getUint16(0x192)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x192, value)

  // Getters and Setters for address 0x2538:0x194/0x25514.
  var word16_2538_0194: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x194)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x194, value)

  // Getters and Setters for address 0x2538:0x196/0x25516.
  var word16_2538_0196: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x196)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x196, value)

  // Getters and Setters for address 0x2538:0x198/0x25518.
  var word16_2538_0198: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x198)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x198, value)

  // Getters and Setters for address 0x2538:0x19A/0x2551A.
  var word16_2538_019A: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x19A)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x19A, value)

  // Getters and Setters for address 0x2538:0x19C/0x2551C.
  var byte8_2538_019C: Int
    // Was accessed via the following registers: CS
    get() = getUint8(0x19C)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0x19C, value)

  // Getters and Setters for address 0x2538:0x19D/0x2551D.
  var word16_2538_019D: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x19D)
    // Operation not registered by running code
    set(value) = setUint16(0x19D, value)

  // Getters and Setters for address 0x2538:0x19E/0x2551E.
  var byte8_2538_019E: Int
    // Was accessed via the following registers: CS
    get() = getUint8(0x19E)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0x19E, value)

  // Getters and Setters for address 0x2538:0x19F/0x2551F.
  var word16_2538_019F_VgaStatusRegisterPort: Int
    // Operation not registered by running code
    get() = getUint16(0x19F)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x19F, value)

  // Getters and Setters for address 0x2538:0x1A1/0x25521.
  var byte8_2538_01A1_NeedWaitForRetrace: Int
    // Operation not registered by running code
    get() = getUint8(0x1A1)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0x1A1, value)

  // Getters and Setters for address 0x2538:0x1A2/0x25522.
  var byte8_2538_01A2_VgaStatusRegisterPortExpectedRetraceValue: Int
    // Operation not registered by running code
    get() = getUint8(0x1A2)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0x1A2, value)

  // Getters and Setters for address 0x2538:0x1A3/0x25523.
  var word16_2538_01A3_VgaOffset: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1A3)
    // Operation not registered by running code
    set(value) = setUint16(0x1A3, value)

  // Getters and Setters for address 0x2538:0x1A5/0x25525.
  var word16_2538_01A5: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1A5)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1A5, value)

  // Getters and Setters for address 0x2538:0x1A7/0x25527.
  var word16_2538_01A7: Int
    // Operation not registered by running code
    get() = getUint16(0x1A7)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1A7, value)

  var dword32_2538_01A7: Int
    // Was accessed via the following registers: CS
    get() = getUint32(0x1A7)
    // Operation not registered by running code
    set(value) = setUint32(0x1A7, value)

  var dword32Ptr_2538_01A7: SegmentedAddress
    // Operation not registered by running code
    get() = SegmentedAddress(getUint16(0x1A7 + 2), getUint16(0x1A7))
    // Operation not registered by running code
    set(value) {
      setUint16(0x1A7 + 2, value.segment)
      setUint16(0x1A7, value.offset)
    }

  // Getters and Setters for address 0x2538:0x1A9/0x25529.
  var word16_2538_01A9: Int
    // Operation not registered by running code
    get() = getUint16(0x1A9)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1A9, value)

  // Getters and Setters for address 0x2538:0x1AB/0x2552B.
  var word16_2538_01AB: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1AB)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1AB, value)

  // Getters and Setters for address 0x2538:0x1AD/0x2552D.
  var word16_2538_01AD: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1AD)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1AD, value)

  // Getters and Setters for address 0x2538:0x1AF/0x2552F.
  var word16_2538_01AF: Int
    // Operation not registered by running code
    get() = getUint16(0x1AF)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1AF, value)

  // Getters and Setters for address 0x2538:0x1B1/0x25531.
  var byte8_2538_01B1: Int
    // Was accessed via the following registers: CS
    get() = getUint8(0x1B1)
    // Operation not registered by running code
    set(value) = setUint8(0x1B1, value)

  var word16_2538_01B1: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1B1)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1B1, value)

  // Getters and Setters for address 0x2538:0x1B2/0x25532.
  var byte8_2538_01B2: Int
    // Was accessed via the following registers: CS
    get() = getUint8(0x1B2)
    // Operation not registered by running code
    set(value) = setUint8(0x1B2, value)

  // Getters and Setters for address 0x2538:0x1B3/0x25533.
  var word16_2538_01B3: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1B3)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1B3, value)

  // Getters and Setters for address 0x2538:0x1B5/0x25535.
  var word16_2538_01B5: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1B5)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1B5, value)

  // Getters and Setters for address 0x2538:0x1B7/0x25537.
  var word16_2538_01B7: Int
    // Operation not registered by running code
    get() = getUint16(0x1B7)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1B7, value)

  // Getters and Setters for address 0x2538:0x1B9/0x25539.
  var word16_2538_01B9: Int
    // Operation not registered by running code
    get() = getUint16(0x1B9)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1B9, value)

  // Getters and Setters for address 0x2538:0x1BB/0x2553B.
  var word16_2538_01BB: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1BB)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1BB, value)

  // Getters and Setters for address 0x2538:0x1BD/0x2553D.
  var byte8_2538_01BD_PaletteLoadMode: Int
    // Operation not registered by running code
    get() = getUint8(0x1BD)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0x1BD, value)

  // Getters and Setters for address 0x2538:0x1BE/0x2553E.
  var byte8_2538_01BE: Int
    // Was accessed via the following registers: CS
    get() = getUint8(0x1BE)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0x1BE, value)

  // Getters and Setters for address 0x2538:0xC3C/0x25FBC.
  var word16_2538_0C3C: Int
    // Operation not registered by running code
    get() = getUint16(0xC3C)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0xC3C, value)

  // Getters and Setters for address 0x2538:0xC43/0x25FC3.
  var byte8_2538_0C43: Int
    // Operation not registered by running code
    get() = getUint8(0xC43)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0xC43, value)

  // Getters and Setters for address 0x2538:0xCD6/0x26056.
  var word16_2538_0CD6: Int
    // Operation not registered by running code
    get() = getUint16(0xCD6)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0xCD6, value)

  // Getters and Setters for address 0x2538:0xCDD/0x2605D.
  var byte8_2538_0CDD: Int
    // Operation not registered by running code
    get() = getUint8(0xCDD)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0xCDD, value)

  // Getters and Setters for address 0x2538:0xD81/0x26101.
  var word16_2538_0D81: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0xD81)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0xD81, value)

  // Getters and Setters for address 0x2538:0xD83/0x26103.
  var word16_2538_0D83: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0xD83)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0xD83, value)

  // Getters and Setters for address 0x2538:0xDDE/0x2615E.
  var word16_2538_0DDE: Int
    // Operation not registered by running code
    get() = getUint16(0xDDE)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0xDDE, value)

  // Getters and Setters for address 0x2538:0xE13/0x26193.
  var byte8_2538_0E13: Int
    // Operation not registered by running code
    get() = getUint8(0xE13)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0xE13, value)

  // Getters and Setters for address 0x2538:0xE26/0x261A6.
  var byte8_2538_0E26: Int
    // Operation not registered by running code
    get() = getUint8(0xE26)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0xE26, value)

  // Getters and Setters for address 0x2538:0xE32/0x261B2.
  var byte8_2538_0E32: Int
    // Operation not registered by running code
    get() = getUint8(0xE32)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0xE32, value)

  // Getters and Setters for address 0x2538:0xEF0/0x26270.
  var byte8_2538_0EF0: Int
    // Operation not registered by running code
    get() = getUint8(0xEF0)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0xEF0, value)

  // Getters and Setters for address 0x2538:0x10DC/0x2645C.
  var word16_2538_10DC: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x10DC)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x10DC, value)

  // Getters and Setters for address 0x2538:0x10DE/0x2645E.
  var word16_2538_10DE: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x10DE)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x10DE, value)

  // Getters and Setters for address 0x2538:0x10E0/0x26460.
  var word16_2538_10E0: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x10E0)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x10E0, value)

  // Getters and Setters for address 0x2538:0x158B/0x2690B.
  var word16_2538_158B: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x158B)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x158B, value)

  // Getters and Setters for address 0x2538:0x16A9/0x26A29.
  var byte8_2538_16A9: Int
    // Operation not registered by running code
    get() = getUint8(0x16A9)
    // Was accessed via the following registers: CS
    set(value) = setUint8(0x16A9, value)

  // Getters and Setters for address 0x2538:0x16AB/0x26A2B.
  var word16_2538_16AB: Int
    // Operation not registered by running code
    get() = getUint16(0x16AB)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x16AB, value)

  // Getters and Setters for address 0x2538:0x1CA0/0x27020.
  var word16_2538_1CA0: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CA0)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CA0, value)

  // Getters and Setters for address 0x2538:0x1CA2/0x27022.
  var word16_2538_1CA2: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CA2)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CA2, value)

  // Getters and Setters for address 0x2538:0x1CA4/0x27024.
  var word16_2538_1CA4: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CA4)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CA4, value)

  // Getters and Setters for address 0x2538:0x1CA6/0x27026.
  var word16_2538_1CA6_UnknownGlobeRelated: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CA6)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CA6, value)

  // Getters and Setters for address 0x2538:0x1CA8/0x27028.
  var word16_2538_1CA8: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CA8)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CA8, value)

  // Getters and Setters for address 0x2538:0x1CAA/0x2702A.
  var word16_2538_1CAA: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CAA)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CAA, value)

  // Getters and Setters for address 0x2538:0x1CAC/0x2702C.
  var word16_2538_1CAC: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CAC)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CAC, value)

  // Getters and Setters for address 0x2538:0x1CAE/0x2702E.
  var word16_2538_1CAE_UnknownGlobeRelated: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CAE)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CAE, value)

  // Getters and Setters for address 0x2538:0x1CB0/0x27030.
  var word16_2538_1CB0_UnknownGlobeRelated: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CB0)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CB0, value)

  // Getters and Setters for address 0x2538:0x1CB2/0x27032.
  var word16_2538_1CB2_UnknownGlobeRelated: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CB2)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CB2, value)

  // Getters and Setters for address 0x2538:0x1CB4/0x27034.
  var word16_2538_1CB4: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x1CB4)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1CB4, value)

  // Getters and Setters for address 0x2538:0x1E4A/0x271CA.
  var word16_2538_1E4A: Int
    // Operation not registered by running code
    get() = getUint16(0x1E4A)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1E4A, value)

  // Getters and Setters for address 0x2538:0x1EA6/0x27226.
  var word16_2538_1EA6_UnknownGlobeRelated: Int
    // Operation not registered by running code
    get() = getUint16(0x1EA6)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1EA6, value)

  // Getters and Setters for address 0x2538:0x1F29/0x272A9.
  var word16_2538_1F29_UnknownGlobeRelated: Int
    // Operation not registered by running code
    get() = getUint16(0x1F29)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x1F29, value)

  // Getters and Setters for address 0x2538:0x2535/0x278B5.
  var word16_2538_2535: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x2535)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x2535, value)

  // Getters and Setters for address 0x2538:0x2537/0x278B7.
  var word16_2538_2537: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x2537)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x2537, value)

  // Getters and Setters for address 0x2538:0x2539/0x278B9.
  var word16_2538_2539: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x2539)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x2539, value)

  // Getters and Setters for address 0x2538:0x261B/0x2799B.
  var word16_2538_261B: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x261B)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x261B, value)

  // Getters and Setters for address 0x2538:0x2768/0x27AE8.
  var word16_2538_2768: Int
    // Operation not registered by running code
    get() = getUint16(0x2768)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x2768, value)

  // Getters and Setters for address 0x2538:0x276A/0x27AEA.
  var word16_2538_276A: Int
    // Operation not registered by running code
    get() = getUint16(0x276A)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x276A, value)

  // Getters and Setters for address 0x2538:0x3114/0x28494.
  var word16_2538_3114: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x3114)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x3114, value)

  // Getters and Setters for address 0x2538:0x3116/0x28496.
  var word16_2538_3116: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x3116)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x3116, value)

  // Getters and Setters for address 0x2538:0x35EA/0x2896A.
  var word16_2538_35EA: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35EA)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35EA, value)

  // Getters and Setters for address 0x2538:0x35EC/0x2896C.
  var word16_2538_35EC: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35EC)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35EC, value)

  // Getters and Setters for address 0x2538:0x35EE/0x2896E.
  var word16_2538_35EE: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35EE)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35EE, value)

  // Getters and Setters for address 0x2538:0x35F0/0x28970.
  var word16_2538_35F0: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35F0)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35F0, value)

  // Getters and Setters for address 0x2538:0x35F2/0x28972.
  var word16_2538_35F2: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35F2)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35F2, value)

  // Getters and Setters for address 0x2538:0x35F4/0x28974.
  var word16_2538_35F4: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35F4)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35F4, value)

  // Getters and Setters for address 0x2538:0x35F6/0x28976.
  var word16_2538_35F6: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35F6)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35F6, value)

  // Getters and Setters for address 0x2538:0x35F8/0x28978.
  var word16_2538_35F8: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35F8)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35F8, value)

  // Getters and Setters for address 0x2538:0x35FA/0x2897A.
  var word16_2538_35FA: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35FA)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35FA, value)

  // Getters and Setters for address 0x2538:0x35FC/0x2897C.
  var word16_2538_35FC: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35FC)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35FC, value)

  // Getters and Setters for address 0x2538:0x35FE/0x2897E.
  var word16_2538_35FE: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x35FE)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x35FE, value)

  // Getters and Setters for address 0x2538:0x3600/0x28980.
  var word16_2538_3600: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x3600)
    // Was accessed via the following registers: CS
    set(value) = setUint16(0x3600, value)

  // Getters and Setters for address 0x2538:0x3A10/0x28D90.
  var word16_2538_3A10: Int
    // Was accessed via the following registers: CS
    get() = getUint16(0x3A10)
    // Operation not registered by running code
    set(value) = setUint16(0x3A10, value)

  // Getters and Setters for address 0x2538:0xCC92/0x32012.
  var dword32Ptr_2538_CC92: SegmentedAddress
    // Was accessed via the following registers: CS
    get() = SegmentedAddress(getUint16(0xCC92 + 2), getUint16(0xCC92))
    // Operation not registered by running code
    set(value) {
      setUint16(0xCC92 + 2, value.segment)
      setUint16(0xCC92, value.offset)
    }
}
